# Local Blender Scene

This directory is for optional local Blender scene files used by
`scripts/render_blender_captures.py`.

The current local workspace may contain `dozer.blend`, but `.blend` files are
ignored by Git because they are large binary assets. The recovered portfolio
workflow does not require this file; it only needs the tracked capture pairs in
`data/captures`.
