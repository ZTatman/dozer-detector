# Local 3D Model Sources

This directory is for optional local model sources and textures used while
experimenting with new Blender capture generation.

Model archives, meshes, and textures are ignored by Git. Keep only assets here
that you have the right to use locally, and document licensing before promoting
any model into a redistributable project artifact.
